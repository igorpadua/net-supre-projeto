import { OrdernarColunaPipe } from './ordernar-coluna.pipe';

describe('OrdernarColunaPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdernarColunaPipe();
    expect(pipe).toBeTruthy();
  });
});
